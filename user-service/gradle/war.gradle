apply plugin: "war"

bootWar {
    mainClass = "com.youthvision.erp.userservice.UserserviceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
